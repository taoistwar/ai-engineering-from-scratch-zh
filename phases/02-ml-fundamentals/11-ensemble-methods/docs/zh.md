# 集成方法

> 一组弱学习器，正确组合后，变成一个强学习器。这不是比喻。这是一条定理。

**类型：** 构建
**语言：** Python
**先决条件：** 阶段 2，第 10 课（偏差-方差权衡）
**时间：** ~120 分钟

## 学习目标

- 从头实现 AdaBoost 和梯度提升，并解释提升如何顺序地减少偏差
- 构建 Bagging 集成，并演示平均去相关模型如何在不增加偏差的情况下减少方差
- 从每种方法针对的错误分量的角度比较 Bagging、Boosting 和 Stacking
- 评估集成多样性，并解释为什么多数投票准确率随着更多独立弱学习器而提高

## 问题

单棵决策树训练快且易于解释，但它过拟合。单个线性模型在复杂边界上欠拟合。你可以花数天设计完美的模型架构。或者，你可以组合一堆不完美的模型，得到比任何单个模型都更好的结果。

集成方法正是这样做的。它们是在表格数据上赢得 Kaggle 竞赛的最可靠技术，它们为大多数生产 ML 系统提供动力，它们展示了偏差-方差权衡的实际运作。Bagging 减少方差。Boosting 减少偏差。Stacking 学习在哪些输入上信任哪些模型。

## 概念

### 为什么集成有效

假设你有 N 个独立的分类器，每个准确率为 p > 0.5。多数投票的准确率为：

```
P(多数正确) = 对 k > N/2 求和 C(N,k) * p^k * (1-p)^(N-k)
```

对于 21 个每个 60% 准确率的分类器，多数投票准确率约为 74%。对于 101 个分类器，上升到 84%。当模型犯不同的错误时，错误互相抵消。

关键要求是**多样性**。如果所有模型犯相同的错误，组合它们毫无帮助。集成之所以有效，是因为它们通过以下方式产生不同的模型：

- 不同的训练子集（Bagging）
- 不同的特征子集（随机森林）
- 顺序的错误纠正（Boosting）
- 不同的模型族（Stacking）

### Bagging（自助聚合）

Bagging 通过在训练数据的不同自助样本上训练每个模型来创造多样性。自助样本是从原始数据中有放回地抽取的，大小与原始相同。约 63.2% 的唯一样本出现在每个自助中。剩余的 36.8%（袋外样本）提供免费的验证集。

Bagging 在不显著增加偏差的情况下减少方差。每棵单独的树对其自助样本过拟合，但过拟合对每棵树不同，因此平均会抵消噪声。

**随机森林**是带有额外变化的 Bagging：在每个分割处，仅考虑随机子集的特征。候选特征数对于分类为 `sqrt(n_features)`，对于回归为 `n_features / 3`。

### Boosting（顺序错误纠正）

Boosting 顺序训练模型。每个新模型专注于先前模型做错的样本。

Boosting 减少偏差。每个新模型纠正集成到目前为止的系统性错误。最终预测是所有模型的加权和，更好的模型获得更高权重。

权衡：如果运行太多轮，Boosting 可能会过拟合，因为它持续拟合更难的样本，其中一些可能是噪声。

### AdaBoost

```
1. 初始化样本权重：w_i = 1/N 对所有 i
2. 对于 t = 1 到 T：
   a. 在加权数据上训练弱学习器 h_t
   b. 计算加权误差：err_t = sum(w_i * I(h_t(x_i) != y_i)) / sum(w_i)
   c. 计算模型权重：alpha_t = 0.5 * ln((1 - err_t) / err_t)
   d. 更新样本权重：w_i = w_i * exp(-alpha_t * y_i * h_t(x_i))
   e. 归一化权重使和为 1
3. 最终预测：H(x) = sign(sum(alpha_t * h_t(x)))
```

误差更低的模型获得更高 alpha。误分类样本获得更高权重，因此下一个模型关注它们。

### 梯度提升

梯度提升将 Boosting 推广到任意损失函数。它不是重加权样本，而是将每个新模型拟合到当前集成残差（损失的负梯度）上。

```
1. 初始化：F_0(x) = argmin_c sum(L(y_i, c))
2. 对于 t = 1 到 T：
   a. 计算伪残差：r_i = -dL(y_i, F_{t-1}(x_i)) / dF_{t-1}(x_i)
   b. 在残差 r_i 上拟合一棵树 h_t
   c. 找到最优步长
   d. 更新：F_t(x) = F_{t-1}(x) + learning_rate * gamma_t * h_t(x)
3. 最终预测：F_T(x)
```

对于平方误差损失，伪残差就是实际残差。学习率控制每棵树的贡献大小。较小的学习率需要更多树但泛化更好。典型值：0.01 到 0.3。

### XGBoost：为什么它在表格数据上占据主导地位

XGBoost 是带有工程优化的梯度提升：
- **正则化目标**：对叶权重的 L1 和 L2 惩罚
- **二阶近似**：使用损失的一阶和二阶导数
- **稀疏感知分割**：原生处理缺失值
- **列子采样**：像随机森林一样在每个分割采样特征
- **加权分位数草图**：在分布式数据上高效找到连续特征的分割点

## 构建它

实现 AdaBoost、Bagging 和梯度提升。完整代码见 `code/ensemble.py`。

## 使用它

```python
from sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier
from xgboost import XGBClassifier

rf = RandomForestClassifier(n_estimators=100)
rf.fit(X_train, y_train)

gb = GradientBoostingClassifier(n_estimators=100, learning_rate=0.1)
gb.fit(X_train, y_train)
```

## 练习

1. 训练 21 个随机分类器，每个 60% 准确率。多数的准确率是多少？与公式匹配。
2. 实现 AdaBoost 并使用深度 1 的树作为弱学习器。以 1 到 100 的范围改变轮次。绘制训练和测试误差 vs 轮次。
3. 比较相同数据上的 Bagging vs Boosting vs 单棵树。每种方法的训练和测试误差是怎样的？偏差和方差呢？

## 关键术语

| 术语 | 实际含义 |
|------|----------------------|
| Bagging | 在自助样本上训练模型并平均它们以减少方差 |
| Boosting | 顺序训练模型，每个纠正前一模型的错误以减少偏差 |
| AdaBoost | 使用加权投票的经典提升算法 |
| 梯度提升 | 通过拟合残差推广到任意损失函数的提升 |
| 多样性 | 当模型犯不同错误时，集成改进的关键要求 |
| 袋外样本 | 未包含在自助样本中的样本，提供服务验证 |
